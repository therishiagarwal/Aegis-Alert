import 'package:flutter/material.dart';

class GovtVerifiedSignupPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Govt. Verified Signup'),
      ),
      body: Center(
        child: Text('Signup with your Govt. Verified ID.'),
      ),
    );
  }
}
